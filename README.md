# AI File Manager

Local-first AI-assisted document organizer and semantic file assistant for Windows.

## Status

Initial scaffold. The application indexes files locally, extracts text/metadata, generates safe filename proposals, and provides a local search/chat interface. Cloud AI is optional; the default design is compatible with Ollama.
